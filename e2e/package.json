{
  "name": "e2e-tests",
  "version": "1.0.0",
  "scripts": {
    "test": "npx folio --config=config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^0.1101.0",
    "axe-playwright": "^1.1.8"
  },
  "dependencies": {
    "dotenv": "^8.2.0"
  }
}